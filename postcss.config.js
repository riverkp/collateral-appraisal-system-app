export default {
  plugins: {
    "@tailwindcss/postcss": {
      // Enable additional features
      form: true, // For form styling
      typography: true, // For typography features
      aspectRatio: true, // For aspect ratio utilities
    },
  },
};
